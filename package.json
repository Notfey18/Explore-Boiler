{
  "scripts": {
    "build": "tsc --noEmit && tsc --outDir build",
    "dev": "npm run build && node build/index.js",
    "start": "nodemon"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.4",
    "typescript": "^5.5.3",
    "@types/express": "^4.17.21"
  }
}
